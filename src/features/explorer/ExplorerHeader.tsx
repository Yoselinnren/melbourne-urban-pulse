import Link from "next/link";import type { ExplorerState } from "./explorer-state";
export default function ExplorerHeader({state,count,currentContext,onFilters,onClear,onReset}:{state:ExplorerState;count:number;currentContext:string;onFilters:()=>void;onClear:()=>void;onReset:()=>void}){return <header className="explorer-header"><div className="brand"><span className="brand-mark">MUP</span><strong>Melbourne Urban Pulse</strong></div><nav aria-label="Primary"><Link href="/projects/melbourne-urban-pulse">Project</Link><span aria-current="page">Explore</span><Link href="/projects/melbourne-urban-pulse/methodology">Method</Link></nav><div className="header-context"><span><b>{count}</b> Pulses in view</span><span>{currentContext}</span></div><div className="header-actions"><button onClick={onFilters} aria-expanded={state.filtersOpen} aria-controls="explorer-filters">Filters</button><button onClick={onClear} disabled={!state.selectedPulseId&&!state.selectedSensorId}>Clear selection</button><button onClick={onReset} disabled={!state.month&&!state.direction&&!state.scopes.length&&!state.evidenceReadiness.length}>Reset filters</button></div></header>}
