export default function ExplorerStatus({kind="loading",children,onRetry}:{kind?:"loading"|"error"|"empty";children:React.ReactNode;onRetry?:()=>void}){return <div className={`explorer-status ${kind}`} role={kind==="error"?"alert":"status"}>{kind==="loading"&&<span className="spinner" aria-hidden="true"/>}<span>{children}</span>{onRetry&&<button onClick={onRetry}>Retry</button>}</div>}
