import type { PulseIndexItem } from "@/lib/types/ui-data";
const lanes=[['network_wide','Network-wide'],['broad','Broad'],['localized','Localized']] as const, months=['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'];
const yearStart=Date.parse('2025-01-01T00:00:00+11:00'),yearMs=365*24*3600e3;
export default function TemporalRibbon({pulses,selected,hovered,onHover,onSelect}:{pulses:PulseIndexItem[];selected:string|null;hovered:string|null;onHover:(id:string|null)=>void;onSelect:(id:string)=>void}){return <section className="timeline" aria-labelledby="timeline-title"><div className="timeline-heading"><div><span className="eyebrow">Annual navigation</span><h2 id="timeline-title">2025 Pulse timeline</h2></div><div className="timeline-legend"><span className="above-key">▲ Above</span><span className="below-key">▼ Below</span><span className="pending-key">• Pending evidence review</span></div></div>{pulses.length===0?<div className="timeline-empty">No Pulses match these filters. Adjust filters to restore the annual view.</div>:<div className="timeline-chart"><div className="month-axis">{months.map(m=><span key={m}>{m}</span>)}</div>{lanes.map(([scope,label])=>{const xs=pulses.filter(p=>p.scope===scope);return <div className="timeline-lane" key={scope}><span className="lane-label">{label}</span><div className="lane-track">{xs.map((p,i)=>{const left=Math.max(0,Math.min(99.5,(Date.parse(p.start)-yearStart)/yearMs*100)),width=Math.max(.45,p.durationHours/8760*100),sub=i%3;const pending=p.evidenceReadiness==='auto_matched_pending_review';return <button key={p.pulseId} className={`pulse-mark ${p.direction} ${selected===p.pulseId?'selected':''} ${hovered===p.pulseId?'hovered':''} ${pending?'pending':''}`} style={{left:`${left}%`,width:`${width}%`,top:`${5+sub*14}px`}} onMouseEnter={()=>onHover(p.pulseId)} onMouseLeave={()=>onHover(null)} onFocus={()=>onHover(p.pulseId)} onBlur={()=>onHover(null)} onClick={()=>onSelect(p.pulseId)} aria-label={`${p.start} to ${p.end}; ${p.direction} expected; ${label}; ${p.durationHours} hours; maximum ${p.maxActiveSensorCount} active sensors; ${p.evidenceReadiness.replaceAll('_',' ')}`}/>} )}</div></div>})}</div>}</section>}
