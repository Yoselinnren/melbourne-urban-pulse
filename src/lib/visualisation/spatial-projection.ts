import type { Coordinates } from "@/lib/types/ui-data";
export function projectCoordinates(points:Coordinates[],width=760,height=470,padding=54){const meanLat=points.reduce((s,p)=>s+p.latitude,0)/points.length,cos=Math.cos(meanLat*Math.PI/180);const raw=points.map(p=>({x:p.longitude*cos,y:-p.latitude}));const xs=raw.map(p=>p.x),ys=raw.map(p=>p.y),minX=Math.min(...xs),maxX=Math.max(...xs),minY=Math.min(...ys),maxY=Math.max(...ys),scale=Math.min((width-padding*2)/(maxX-minX),(height-padding*2)/(maxY-minY));return raw.map(p=>({x:padding+(p.x-minX)*scale+(width-padding*2-(maxX-minX)*scale)/2,y:padding+(p.y-minY)*scale+(height-padding*2-(maxY-minY)*scale)/2}))}
