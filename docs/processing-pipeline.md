# Processing Pipeline

Generated by `scripts/build_mvp_dashboard_data.py`.

## Inputs

| Source | Role |
| --- | --- |
| `data/raw/pedestrian/pedestrian_counts_hourly_full.csv` | observed hourly pedestrian counts |
| `data/raw/sensors/pedestrian_sensor_locations.csv` | sensor metadata and coordinates |
| `data/raw/weather/open_meteo_melbourne_hourly_2025.json` | primary hourly weather context |
| `data/raw/calendar/victoria_important_dates_2025.csv` | calendar and important-date context |

Planned works are intentionally auxiliary only for this MVP because profiling showed the local source does not align cleanly with 2025.

## Study Period

The MVP is filtered to **2025-01-01 00:00 through 2025-12-31 23:00** in the `Australia/Melbourne` timezone.

## Selected Sensors

| Sensor ID | Label | Role |
| --- | --- | --- |
| 4 | Town Hall | selected MVP sensor |
| 3 | Melbourne Central | selected MVP sensor |
| 133 | Southern Cross | selected MVP sensor |

## Transformations

1. Read raw pedestrian counts without modifying `data/raw`.
2. Filter pedestrian rows to 2025 and MVP sensors `4`, `3`, and `133`.
3. Read sensor metadata and attach names, coordinates, status, and display labels.
4. Read Open-Meteo weather JSON as the primary weather source.
5. Read Victorian important dates and derive daily calendar flags.
6. Generate one hourly dashboard record for every hour in 2025.
7. Keep missing pedestrian observations explicit with `observed_count: null` and `is_missing: true`.
8. Compute provisional baseline and signal indicators.
9. Write identical JSON to `data/processed/` and `public/dashboard-data/`.

## Provisional Metrics

- `baseline_count`: median count by `sensor_id + weekday + hour` within selected 2025 data.
- `activity_intensity`: observed count normalised by the selected sensor's 2025 maximum.
- `baseline_deviation`: `(observed_count - baseline_count) / baseline_count`.
- `anomaly_score`: IQR-scaled absolute deviation, capped to 1.
- `pulse_score`: simple prototype blend of activity intensity and anomaly score.

These indicators are suitable for a first vertical slice but should be documented as provisional.

## Row Counts

| Metric | Count |
| --- | ---: |
| Pedestrian raw rows scanned | 1607116 |
| Pedestrian rows in 2025 | 816028 |
| Pedestrian rows for selected MVP sensors | 26277 |
| Weather hourly rows | 8760 |
| Calendar raw rows scanned | 2879 |
| Calendar rows in 2025 | 381 |
| Output hourly records | 8760 |
| Output sensor readings | 26280 |
| Explicit missing sensor readings | 3 |

## Outputs

| Output | Notes |
| --- | --- |
| `data/processed/mvp_dashboard_data.json` | full-year local analysis output |
| `public/dashboard-data/mvp_dashboard_data.json` | full-year public export retained for validation and future delivery decisions |
| `public/dashboard-data/mvp_dashboard_compact.json` | size-controlled 1–7 January extract for frontend vertical-slice and demo loading |

Output JSON size: **15,272,796 bytes**.

### Public Dataset Size Control

The full processed dataset is the canonical local analysis output. The compact
public dataset preserves the dashboard contract, selected sensors, hourly
weather and calendar context, explanation cards, pulse-field frames, and
explicit missing-value semantics for a representative seven-day window. It
also carries computed full-year record and sensor-coverage summaries in
`metadata.full_dataset_summary`.

Frontend demo data should remain size-controlled because a large statically
imported JSON file increases the JavaScript bundle, build memory use, transfer
time, and initial parsing cost. The vertical slice therefore loads the compact
file; production delivery of the full year should later use partitioned or
on-demand data rather than bundling the complete 15 MB export.

## Assumptions and Limitations

- Missing observations are not treated as observed zero activity.
- Calendar flags are derived from explicit `important_date` rows; multi-day range expansion is not implemented yet.
- Planned works are not joined because the current local source does not provide clean 2025 coverage.
- Event explanation cards remain placeholders until manual event annotation is completed.
- No spatial interpolation or real edge AI inference is implemented.
